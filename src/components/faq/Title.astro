---
import { Typography } from "@/components/ui/Typography";
import { cn } from "@/lib/utils";

interface Props {
  className?: string;
}

const { className } = Astro.props;
---

<Typography variant="display" size="sm" className={cn("mb-2 py-4", className)}>
  <slot />
</Typography>
